import { render, screen } from '@testing-library/react'
import App from './App'

test('renders app', () => {
  // This is how it should work but charka might be an issue
  // render(<App />)
  // const linkElement = screen.getAllByText(/Wrong Network/i)
  // expect(linkElement.length).toBe(2)
})
