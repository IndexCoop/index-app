export function useLedgerStatus() {
  // TODO:
  // const searchParams = new URLSearchParams(window.location.search)
  // const isRunningInLedgerLive = searchParams.get('embedded') === 'true'
  return { isRunningInLedgerLive: false }
}
